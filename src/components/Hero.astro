<div class="hero">
  <div class="avatar">
    <span class="ring"></span>
    <span class="monogram"
      >r<span style="color: var(--accent-ink)">a</span></span
    >
  </div>
  <div class="hero-id">
    <h1>
      <span id="name-glitch" data-text="Ren Alvaro">Vaibhav Singh</span>
      <span class="verified" title="Verified human"
        ><svg
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="3"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <path d="m5 12 5 5L20 7"></path></svg
        ></span
      >
    </h1>
    <div class="tagline" id="typewriter">Systems · ML · Interfaces</div>
    <div class="status">
      <span class="live"></span><span
        >online · shipping from a train somewhere</span
      >
    </div>
  </div>
  <div class="hero-meta">
    <svg
      width="12"
      height="12"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      stroke-width="2"
    >
      <path d="M2 12s3-7 10-7 10 7 10 7-3 7-10 7S2 12 2 12Z"></path>
      <circle cx="12" cy="12" r="3"></circle>
    </svg>
    <span id="views">12,408</span>
  </div>
</div>

<script>import '../scripts/typewriter.ts'</script>
<script>import '../scripts/motion-hero.ts'</script>

<style>
  .hero {
    display: grid;
    grid-template-columns: 112px minmax(0, 1fr) auto;
    gap: 22px;
    align-items: start;
    padding-top: 28px;
  }
  .hero-id {
    min-width: 0;
    padding-top: 8px;
  }
  .avatar {
    width: 112px;
    height: 112px;
    border-radius: 14px;
    border: 1px solid var(--rule);
    background: var(--card);
    overflow: hidden;
    position: relative;
    box-shadow: var(--shadow);
    display: grid;
    place-items: center;
  }
  .avatar::after {
    content: "";
    position: absolute;
    inset: 0;
    background-image: repeating-linear-gradient(
      45deg,
      var(--hatch) 0 4px,
      transparent 4px 10px
    );
    opacity: 0.7;
  }
  .avatar .monogram {
    position: relative;
    z-index: 1;
    font-family: "Instrument Serif", serif;
    font-style: italic;
    font-size: 58px;
    color: var(--ink);
    letter-spacing: -0.04em;
  }
  .avatar .ring {
    position: absolute;
    inset: -6px;
    border: 1px dashed var(--rule);
    border-radius: 16px;
    animation: spin 40s linear infinite;
  }
  @keyframes spin {
    to {
      transform: rotate(360deg);
    }
  }
  .hero-id .row {
    display: flex;
    align-items: center;
    gap: 10px;
  }
  .hero-id .mono-label {
    font-family: "JetBrains Mono", monospace;
    font-size: 11px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--ink-3);
  }
  .hero-id h1 {
    font-family: "Instrument Serif", serif;
    font-weight: 400;
    font-size: clamp(34px, 4.4vw, 46px);
    letter-spacing: -0.02em;
    margin: 2px 0 8px;
    line-height: 1.05;
    display: flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
  }
  .verified {
    width: 20px;
    height: 20px;
    color: #fff;
    background: var(--verify);
    border-radius: 999px;
    display: grid;
    place-items: center;
    flex: none;
  }
  .verified svg {
    width: 12px;
    height: 12px;
  }
  .hero-id .tagline {
    font-family: "JetBrains Mono", monospace;
    color: var(--ink-2);
    font-size: 13px;
  }
  .status {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-family: "JetBrains Mono", monospace;
    font-size: 12px;
    color: var(--ink-3);
    margin-top: 6px;
  }
  .status .live {
    width: 7px;
    height: 7px;
    border-radius: 999px;
    background: #2ca76c;
    box-shadow: 0 0 0 0 #2ca76c80;
    animation: pulse 2s ease-out infinite;
  }
  @keyframes pulse {
    0% {
      box-shadow: 0 0 0 0 #2ca76c80;
    }
    100% {
      box-shadow: 0 0 0 10px #2ca76c00;
    }
  }
  .hero-meta {
    font-family: "JetBrains Mono", monospace;
    font-size: 12px;
    color: var(--ink-3);
    display: flex;
    align-items: center;
    gap: 6px;
    padding-top: 10px;
    white-space: nowrap;
  }

  @media (max-width: 680px) {
    .hero {
      grid-template-columns: 72px 1fr;
    }
    .hero-meta {
      grid-column: 1/-1;
      padding-top: 0;
    }
    .avatar {
      width: 72px;
      height: 72px;
    }
    .avatar .monogram {
      font-size: 36px;
    }
    .hero-id h1 {
      font-size: 36px;
    }
  }
</style>
