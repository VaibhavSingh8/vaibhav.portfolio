<section id="activity">
  <h2 class="section">GitHub Activity</h2>
  <div class="contrib-wrap">
    <div class="contrib-head">
      <span
        >Last 12 months · <span id="commit-count">612</span> activities in
        2026</span
      >
      <span class="today"><span class="d"></span>Shipped today</span>
    </div>
    <div class="contrib">
      <div class="days">
        <span>&nbsp;</span><span>Mon</span><span>&nbsp;</span
        ><span>Wed</span><span>&nbsp;</span><span>Fri</span
        ><span>&nbsp;</span>
      </div>
      <div>
        <div class="months" id="months"></div>
        <div class="weeks" id="weeks"></div>
      </div>
    </div>
    <div class="contrib-foot">
      <span>Longest streak: 21 days</span>
      <span class="legend">
        Less
        <span class="cell"></span>
        <span class="cell l1"></span>
        <span class="cell l2"></span>
        <span class="cell l3"></span>
        <span class="cell l4"></span>
        More
      </span>
    </div>
  </div>
</section>

<script>import '../scripts/contrib-grid.ts'</script>

<style>
  section {
    margin-top: 36px;
    scroll-margin-top: 80px;
  }
  h2.section {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 32px;
    letter-spacing: -0.015em;
    margin: 0 0 16px;
  }
  .contrib-wrap {
    border: 1px solid var(--rule);
    background: var(--card);
    border-radius: 14px;
    padding: 16px 18px;
    box-shadow: var(--shadow);
    overflow-x: auto;
  }
  .contrib-head {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 6px;
    font-family: var(--font-mono);
    font-size: 11.5px;
    color: var(--ink-3);
  }
  .contrib-head .today {
    display: inline-flex;
    align-items: center;
    gap: 6px;
  }
  .contrib-head .today .d {
    width: 6px;
    height: 6px;
    border-radius: 999px;
    background: var(--ink-3);
  }
  .contrib {
    display: grid;
    grid-template-columns: 18px 1fr;
    gap: 6px;
    min-width: 640px;
  }
  .contrib .days {
    display: grid;
    grid-template-rows: repeat(7, 11px);
    gap: 3px;
    font-family: var(--font-mono);
    font-size: 9.5px;
    color: var(--ink-3);
    padding-top: 16px;
  }
  .contrib .days span {
    height: 11px;
    line-height: 11px;
  }
  .contrib .months {
    display: grid;
    grid-template-columns: repeat(53, 13px);
    gap: 0;
    font-family: var(--font-mono);
    font-size: 9.5px;
    color: var(--ink-3);
    margin-bottom: 4px;
  }
  .contrib .weeks {
    display: grid;
    grid-template-columns: repeat(53, 11px);
    gap: 3px;
  }
  :global(.week) {
    display: grid;
    grid-template-rows: repeat(7, 11px);
    gap: 3px;
  }
  .cell,
  :global(.cell) {
    width: 11px;
    height: 11px;
    border-radius: 2px;
    background: var(--rule);
    cursor: pointer;
    transition:
      outline 0.15s,
      transform 0.15s;
  }
  .cell:hover,
  :global(.cell:hover) {
    outline: 1px solid var(--accent);
    outline-offset: 1px;
    transform: scale(1.3);
  }
  :global(.cell.l1) {
    background: color-mix(in oklab, var(--ink) 12%, var(--bg));
  }
  :global(.cell.l2) {
    background: color-mix(in oklab, var(--ink) 28%, var(--bg));
  }
  :global(.cell.l3) {
    background: color-mix(in oklab, var(--ink) 50%, var(--bg));
  }
  :global(.cell.l4) {
    background: var(--ink);
  }
  .contrib-foot {
    display: flex;
    justify-content: space-between;
    margin-top: 10px;
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--ink-3);
    align-items: center;
  }
  .legend {
    display: inline-flex;
    align-items: center;
    gap: 6px;
  }
  .legend .cell {
    width: 10px;
    height: 10px;
    cursor: default;
  }
  .legend .cell:hover {
    outline: none;
    transform: none;
  }
</style>
