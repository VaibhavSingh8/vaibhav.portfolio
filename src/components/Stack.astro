<section id="stack">
  <h2 class="section">Tech Stack</h2>
  <p class="stack-sub">
    tools I reach for when I&rsquo;m actually shipping.
  </p>

  <div class="marquee" data-speed="48">
    <div class="track" data-row="1"></div>
  </div>
  <div class="marquee" data-speed="64" data-dir="reverse">
    <div class="track" data-row="2"></div>
  </div>
</section>

<script>import '../scripts/carousel.ts'</script>

<style>
  section {
    margin-top: 36px;
    scroll-margin-top: 80px;
  }
  h2.section {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 32px;
    letter-spacing: -0.015em;
    margin: 0 0 16px;
  }
  .stack-sub {
    font-family: var(--font-mono);
    font-size: 11.5px;
    color: var(--ink-3);
    margin: -4px 0 18px;
  }
  .marquee {
    overflow: hidden;
    position: relative;
    padding: 6px 0;
    mask-image: linear-gradient(
      to right,
      transparent,
      #000 8%,
      #000 92%,
      transparent
    );
    -webkit-mask-image: linear-gradient(
      to right,
      transparent,
      #000 8%,
      #000 92%,
      transparent
    );
  }
  .marquee + .marquee {
    margin-top: 10px;
  }
  .marquee .track {
    display: flex;
    gap: 10px;
    width: max-content;
    animation: marquee linear infinite;
    animation-duration: calc(var(--speed, 50) * 1s);
  }
  .marquee[data-dir="reverse"] .track {
    animation-direction: reverse;
  }
  .marquee:hover .track {
    animation-play-state: paused;
  }
  @keyframes marquee {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(-50%);
    }
  }
  :global(.stack-chip) {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 14px;
    border: 1px solid var(--rule);
    border-radius: 999px;
    background: var(--card);
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--ink);
    white-space: nowrap;
    transition:
      border-color 0.2s,
      color 0.2s,
      background 0.2s,
      transform 0.2s;
    flex: none;
  }
  :global(.stack-chip:hover) {
    border-color: var(--accent);
    color: var(--accent-ink);
    transform: translateY(-1px);
  }
  :global(.stack-chip .sicon) {
    display: inline-block;
    width: 14px;
    height: 14px;
    mask-size: contain;
    mask-repeat: no-repeat;
    mask-position: center;
    -webkit-mask-size: contain;
    -webkit-mask-repeat: no-repeat;
    -webkit-mask-position: center;
    background: var(--ink-3);
    transition: background 0.2s;
    flex: none;
  }
  :global(.marquee:hover .stack-chip .sicon) {
    background: var(--chip-color, var(--accent-ink));
  }
  :global([data-theme="dark"] .marquee:hover .stack-chip .sicon) {
    background: var(--chip-color-dark, var(--chip-color, var(--accent-ink)));
  }
  :global(.stack-chip .meta) {
    font-size: 10.5px;
    color: var(--ink-3);
    letter-spacing: 0.06em;
    padding-left: 6px;
    margin-left: 2px;
    border-left: 1px solid var(--rule-2);
  }
</style>
