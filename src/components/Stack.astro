<section id="stack">
  <h2 class="section">Tech Stack</h2>
  <p class="stack-sub">
    tools I reach for when I&rsquo;m actually shipping — hover to pause.
  </p>

  <div class="marquee" data-speed="48">
    <div class="track" data-row="1"></div>
  </div>
  <div class="marquee" data-speed="64" data-dir="reverse">
    <div class="track" data-row="2"></div>
  </div>
  <div class="marquee" data-speed="56">
    <div class="track" data-row="3"></div>
  </div>
</section>

<script>import '../scripts/carousel.ts'</script>

<style>
  section {
    margin-top: 36px;
    scroll-margin-top: 80px;
  }
  h2.section {
    font-family: "Instrument Serif", serif;
    font-weight: 400;
    font-size: 32px;
    letter-spacing: -0.015em;
    margin: 0 0 16px;
  }
  .stack-sub {
    font-family: "JetBrains Mono", monospace;
    font-size: 11.5px;
    color: var(--ink-3);
    margin: -4px 0 18px;
  }
  .marquee {
    overflow: hidden;
    position: relative;
    padding: 6px 0;
    mask-image: linear-gradient(
      to right,
      transparent,
      #000 8%,
      #000 92%,
      transparent
    );
    -webkit-mask-image: linear-gradient(
      to right,
      transparent,
      #000 8%,
      #000 92%,
      transparent
    );
  }
  .marquee + .marquee {
    margin-top: 10px;
  }
  .marquee .track {
    display: flex;
    gap: 10px;
    width: max-content;
    animation: marquee linear infinite;
    animation-duration: calc(var(--speed, 50) * 1s);
  }
  .marquee[data-dir="reverse"] .track {
    animation-direction: reverse;
  }
  .marquee:hover .track {
    animation-play-state: paused;
  }
  @keyframes marquee {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(-50%);
    }
  }
  :global(.stack-chip) {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 14px;
    border: 1px solid var(--rule);
    border-radius: 999px;
    background: var(--card);
    font-family: "JetBrains Mono", monospace;
    font-size: 12px;
    color: var(--ink);
    white-space: nowrap;
    transition:
      border-color 0.2s,
      color 0.2s,
      background 0.2s,
      transform 0.2s;
    flex: none;
  }
  :global(.stack-chip:hover) {
    border-color: var(--accent);
    color: var(--accent-ink);
    transform: translateY(-1px);
  }
  :global(.stack-chip .sdot) {
    width: 6px;
    height: 6px;
    border-radius: 999px;
    opacity: 0.9;
  }
  :global(.stack-chip[data-kind="lang"] .sdot) {
    background: oklch(0.68 0.14 55);
  }
  :global(.stack-chip[data-kind="fwk"] .sdot) {
    background: oklch(0.66 0.13 200);
  }
  :global(.stack-chip[data-kind="infra"] .sdot) {
    background: oklch(0.62 0.1 300);
  }
  :global(.stack-chip[data-kind="tool"] .sdot) {
    background: oklch(0.55 0.02 90);
  }
  :global(.stack-chip .meta) {
    font-size: 10.5px;
    color: var(--ink-3);
    letter-spacing: 0.06em;
    padding-left: 6px;
    margin-left: 2px;
    border-left: 1px solid var(--rule-2);
  }
</style>
