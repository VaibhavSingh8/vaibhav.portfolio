<section id="about">
  <h2 class="section">About</h2>
  <ul class="bullets">
    <li>
      I build quiet tools for loud problems — lately that means inference
      pipelines, agentic glue, and interfaces that stay out of the way
      until they matter.
    </li>
    <li>
      Trained as an engineer, rewired by design. I care about latency,
      line-height, and the honesty of a good error message.
    </li>
    <li>
      Currently independent: shipping
      <span
        style="
          color: var(--accent-ink);
          border-bottom: 1px dashed var(--accent);
        "
        >sous/chef</span
      >
      (a local-first ML runtime) and writing about tools that respect
      their operator.
    </li>
  </ul>

  <div class="currently" aria-label="Currently">
    <div class="card">
      <svg
        class="glyph"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="1.6"
        stroke-linecap="round"
        stroke-linejoin="round"
      >
        <path
          d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"
        />
        <circle cx="12" cy="12" r="3" />
      </svg>
      <span class="k">exploring</span>
      <div class="v">agentic RAG</div>
      <div class="sub">
        <span>local LLMs · tool-use loops</span
        ><span class="t">side-quest</span>
      </div>
    </div>
    <div class="card">
      <svg
        class="glyph"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="1.6"
        stroke-linecap="round"
        stroke-linejoin="round"
      >
        <rect x="4" y="10" width="16" height="11" rx="2" />
        <path d="M8 10V7a4 4 0 0 1 8 0v3" />
      </svg>
      <span class="k">working on</span>
      <div class="v">something I can&rsquo;t talk about</div>
      <div class="sub">
        <span>under NDA · shipping</span><span class="t">day job</span>
      </div>
    </div>
    <div class="card">
      <svg
        class="glyph"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="1.6"
        stroke-linecap="round"
        stroke-linejoin="round"
      >
        <circle cx="12" cy="12" r="9" />
        <path
          d="M4 9h16M4 15h16M12 3a14 14 0 0 1 0 18M12 3a14 14 0 0 0 0 18"
        />
      </svg>
      <span class="k">training</span>
      <div class="v">badminton &middot; gym</div>
      <div class="sub">
        <span>3&times; a week · rest = iron</span
        ><span class="t">court</span>
      </div>
    </div>
    <div class="card">
      <svg
        class="glyph"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="1.6"
        stroke-linecap="round"
        stroke-linejoin="round"
      >
        <path d="M9 18V5l12-2v13" />
        <circle cx="6" cy="18" r="3" />
        <circle cx="18" cy="16" r="3" />
      </svg>
      <span class="k">listening</span>
      <div class="v">Kairos</div>
      <div class="sub">
        <span>Nils Frahm · All Melody</span><span class="t">on loop</span>
      </div>
    </div>
  </div>
</section>

<style>
  section {
    margin-top: 36px;
    scroll-margin-top: 80px;
  }
  h2.section {
    font-family: "Instrument Serif", serif;
    font-weight: 400;
    font-size: 32px;
    letter-spacing: -0.015em;
    margin: 0 0 16px;
  }
  ul.bullets {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 14px;
  }
  ul.bullets li {
    font-family: "JetBrains Mono", monospace;
    font-size: 13.5px;
    line-height: 1.7;
    color: var(--ink-2);
    display: grid;
    grid-template-columns: 14px 1fr;
    gap: 10px;
  }
  ul.bullets li::before {
    content: "";
    width: 5px;
    height: 5px;
    border-radius: 999px;
    background: var(--ink-3);
    margin-top: 12px;
  }
  .currently {
    margin-top: 28px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
  }
  .currently .card {
    position: relative;
    padding: 16px 18px;
    border: 1px solid var(--rule);
    border-radius: 14px;
    background: var(--card);
    display: grid;
    grid-template-rows: auto 1fr auto;
    gap: 8px;
    transition:
      border-color 0.25s,
      transform 0.25s,
      box-shadow 0.25s;
    min-height: 112px;
    overflow: hidden;
  }
  .currently .card::before {
    content: "";
    position: absolute;
    inset: 0;
    background: radial-gradient(
      400px 120px at 100% 0%,
      color-mix(in oklab, var(--accent) 10%, transparent),
      transparent 60%
    );
    opacity: 0;
    transition: opacity 0.3s;
    pointer-events: none;
  }
  .currently .card:hover {
    border-color: color-mix(in oklab, var(--accent) 55%, var(--rule));
    transform: translateY(-2px);
    box-shadow: 0 8px 24px -18px rgba(0, 0, 0, 0.28);
  }
  .currently .card:hover::before {
    opacity: 1;
  }
  .currently .k {
    font-family: "JetBrains Mono", monospace;
    font-size: 10px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--ink-3);
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }
  .currently .k::before {
    content: "";
    width: 5px;
    height: 5px;
    background: var(--accent);
    border-radius: 999px;
    opacity: 0.85;
  }
  .currently .v {
    font-family: "Instrument Serif", serif;
    font-style: italic;
    font-weight: 400;
    font-size: 26px;
    line-height: 1.1;
    color: var(--ink);
    letter-spacing: -0.01em;
  }
  .currently .sub {
    font-family: "JetBrains Mono", monospace;
    font-size: 11px;
    color: var(--ink-3);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
  }
  .currently .sub .t {
    color: var(--ink-3);
    padding: 2px 8px;
    border: 1px solid var(--rule);
    border-radius: 999px;
    font-size: 10px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    white-space: nowrap;
  }
  .currently .glyph {
    position: absolute;
    top: 14px;
    right: 16px;
    width: 22px;
    height: 22px;
    color: var(--ink-3);
    opacity: 0.4;
    transition:
      opacity 0.25s,
      color 0.25s,
      transform 0.25s;
  }
  .currently .card:hover .glyph {
    opacity: 1;
    color: var(--accent-ink);
    transform: rotate(8deg) scale(1.08);
  }
  @media (max-width: 640px) {
    .currently {
      grid-template-columns: 1fr;
    }
  }
</style>
