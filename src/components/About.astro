<section id="about">
  <h2 class="section">About</h2>
  <ul class="bullets">
    <li>
      Software engineer with 3+ years building production web applications
      and AI-powered platforms. I work across the stack, frontend and
      backend, and pick up whatever the problem needs.
    </li>
    <li>
      Lately that means agentic interfaces, MLOps pipelines, payments and
      credits infrastructure, and backend migrations nobody wanted to touch.
      I own things end to end, from understanding the requirement to
      shipping and keeping it alive in production.
    </li>
    <li>
      I think before I type. For a feature that means questioning what's
      actually needed before writing a line. For a bug it means finding
      the root cause, not just the symptom. That curiosity pushes me to
      go deeper than the task in front of me.
    </li>
  </ul>

  <div class="currently" aria-label="Currently">
    <div class="row">
      <span class="k">exploring</span>
      <span class="arrow">→</span>
      <span class="v">AI tools &amp; APIs</span>
      <span class="detail">claude · gpt · building &amp; breaking things</span>
      <span class="t">side-quest</span>
    </div>
    <div class="row">
      <span class="k">building</span>
      <span class="arrow">→</span>
      <span class="v">prompt manager</span>
      <span class="detail">carry context across AIs · chrome ext</span>
      <span class="t">wip</span>
    </div>
    <div class="row">
      <span class="k">training</span>
      <span class="arrow">→</span>
      <span class="v">badminton &middot; gym</span>
      <span class="detail">gym 5&times; · badminton 2&times;</span>
      <span class="t">court</span>
    </div>
    <div class="row">
      <span class="k">listening</span>
      <span class="arrow">→</span>
      <span class="v">lofi work music</span>
      <span class="detail">always on · no distractions</span>
      <span class="t">on loop</span>
    </div>
  </div>
</section>

<style>
  section {
    margin-top: 36px;
    scroll-margin-top: 80px;
  }
  h2.section {
    font-family: "Instrument Serif", serif;
    font-weight: 400;
    font-size: 32px;
    letter-spacing: -0.015em;
    margin: 0 0 16px;
  }
  ul.bullets {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 14px;
  }
  ul.bullets li {
    font-family: "JetBrains Mono", monospace;
    font-size: 13.5px;
    line-height: 1.7;
    color: var(--ink-2);
    display: grid;
    grid-template-columns: 14px 1fr;
    gap: 10px;
  }
  ul.bullets li::before {
    content: "";
    width: 5px;
    height: 5px;
    border-radius: 999px;
    background: var(--ink-3);
    margin-top: 12px;
  }
  .currently {
    margin-top: 28px;
    border: 1px solid var(--rule);
    border-radius: 14px;
    background: var(--card);
    overflow: hidden;
  }
  .currently .row {
    display: grid;
    grid-template-columns: 90px 16px 1fr auto auto;
    align-items: center;
    gap: 10px;
    padding: 13px 18px;
    border-bottom: 1px solid var(--rule-2);
    transition: background 0.2s;
    font-family: "JetBrains Mono", monospace;
    font-size: 12.5px;
  }
  .currently .row:last-child {
    border-bottom: none;
  }
  .currently .row:hover {
    background: color-mix(in oklab, var(--accent) 5%, transparent);
  }
  .currently .k {
    color: var(--ink-3);
    font-size: 11px;
    letter-spacing: 0.06em;
    white-space: nowrap;
  }
  .currently .arrow {
    color: var(--accent-ink);
    opacity: 0.6;
    font-size: 13px;
  }
  .currently .v {
    font-family: "Instrument Serif", serif;
    font-style: italic;
    font-size: 18px;
    color: var(--ink);
    letter-spacing: -0.01em;
    line-height: 1.2;
  }
  .currently .detail {
    font-size: 11px;
    color: var(--ink-3);
    white-space: nowrap;
  }
  .currently .t {
    font-size: 10px;
    color: var(--ink-3);
    padding: 2px 8px;
    border: 1px solid var(--rule);
    border-radius: 999px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    white-space: nowrap;
  }
  @media (max-width: 640px) {
    .currently .row {
      grid-template-columns: 1fr auto;
      grid-template-rows: auto auto;
    }
    .currently .k { grid-column: 1; grid-row: 1; }
    .currently .t { grid-column: 2; grid-row: 1; }
    .currently .arrow { display: none; }
    .currently .v { grid-column: 1; grid-row: 2; font-size: 16px; }
    .currently .detail { display: none; }
  }
</style>
