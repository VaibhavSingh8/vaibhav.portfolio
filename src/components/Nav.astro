<nav class="top">
  <div class="nav-inner">
    <!-- <a class="brand" href="#"
      ><span class="dot"></span>vs<span
        style="color: var(--ink-3); font-weight: 400">/</span
      >vaibhav</a
    > -->

    <a class="brand" href="#">vaibhav</a>

    <div class="nav-links">
      <a href="#about" class="nav-a active">Home</a>
      <a href="#projects" class="nav-a">Projects</a>
      <a href="#writing" class="nav-a">Writing</a>
      <a href="#more" class="nav-a">More</a>
    </div>
    <div style="display: flex; gap: 8px; align-items: center">
      <button class="kbd" id="cmdk" aria-label="Search">
        <svg
          width="12"
          height="12"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
        >
          <circle cx="11" cy="11" r="7"></circle>
          <path d="m21 21-4.3-4.3"></path></svg
        ><kbd>⌘</kbd><kbd>K</kbd>
      </button>
      <button class="theme-btn" id="theme" aria-label="Toggle theme">
        <svg
          class="sun"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <circle cx="12" cy="12" r="4"></circle>
          <path
            d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.41-1.41M17.66 6.34l1.41-1.41"
          ></path>
        </svg>
        <svg
          class="moon"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79Z"></path>
        </svg>
      </button>
    </div>
  </div>
</nav>

<script>
  import "../scripts/theme.ts";
</script>

<style>
  nav.top {
    position: sticky;
    top: 0;
    z-index: 50;
    backdrop-filter: blur(18px) saturate(120%);
    -webkit-backdrop-filter: blur(18px) saturate(120%);
    background: color-mix(in oklab, var(--bg) 75%, transparent);
    border-bottom: 1px solid var(--rule-2);
  }
  .nav-inner {
    max-width: 1080px;
    margin: 0 auto;
    padding: 14px clamp(40px, 10vw, 160px);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
  }
  .brand {
    font-family: var(--font-mono);
    font-weight: 600;
    letter-spacing: -0.02em;
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 18px;
    text-decoration: none;
    color: var(--ink);
  }
  .brand .dot {
    width: 8px;
    height: 8px;
    border-radius: 2px;
    background: var(--accent);
    display: inline-block;
  }
  .nav-links {
    display: flex;
    align-items: center;
    gap: 4px;
  }
  .nav-links a {
    position: relative;
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--ink-2);
    text-decoration: none;
    padding: 8px 12px;
    border-radius: 8px;
    transition:
      color 0.2s,
      background 0.2s;
  }
  .nav-links a:hover {
    color: var(--ink);
    background: var(--rule-2);
  }
  .nav-links a.active {
    color: var(--ink);
  }
  .nav-links a.active::after {
    content: "";
    position: absolute;
    left: 12px;
    right: 12px;
    bottom: 4px;
    height: 1px;
    background: var(--ink);
  }
  .kbd {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 6px 10px;
    border-radius: 999px;
    border: 1px solid var(--rule);
    background: var(--card);
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--ink-2);
    cursor: pointer;
    transition:
      transform 0.2s,
      box-shadow 0.2s;
  }
  .kbd:hover {
    transform: translateY(-1px);
    box-shadow: var(--shadow);
  }
  .kbd kbd {
    font: inherit;
    color: var(--ink-3);
    padding: 0 4px;
    border-radius: 4px;
    border: 1px solid var(--rule);
  }
  .theme-btn {
    width: 38px;
    height: 38px;
    border-radius: 999px;
    border: 1px solid var(--rule);
    background: var(--card);
    display: grid;
    place-items: center;
    cursor: pointer;
    color: var(--ink);
    transition:
      transform 0.2s,
      box-shadow 0.2s,
      background 0.2s;
    position: relative;
    overflow: hidden;
  }
  .theme-btn:hover {
    transform: translateY(-1px);
    box-shadow: var(--shadow);
  }
  .theme-btn svg {
    width: 18px;
    height: 18px;
    transition: transform 0.5s cubic-bezier(0.22, 1, 0.36, 1);
  }
  .theme-btn .sun {
    display: block;
  }
  .theme-btn .moon {
    display: none;
  }
  :global([data-theme="dark"]) .theme-btn .sun {
    display: none;
  }
  :global([data-theme="dark"]) .theme-btn .moon {
    display: block;
  }
  .theme-btn:active svg {
    transform: rotate(-30deg) scale(0.9);
  }
</style>
